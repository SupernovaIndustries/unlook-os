Filled by build.sh at build time (git-ignored content):
- `driver/`, `overlay/`, `driver.commit` — from the `drivers/mira220-sync` submodule
- `debs/unlook-libcamera_*_arm64.deb` — from `libcamera/build-deb.sh`
